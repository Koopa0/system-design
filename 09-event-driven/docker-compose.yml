version: '3.8'

services:
  nats:
    image: nats:2.10-alpine
    container_name: event-driven-nats
    ports:
      - "4222:4222"
      - "8222:8222"
    command: --jetstream --store_dir=/data --http_port=8222
    volumes:
      - nats-data:/data

volumes:
  nats-data:
